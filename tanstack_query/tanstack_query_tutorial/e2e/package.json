{
  "name": "e2e",
  "type": "module",
  "scripts": {
    "test": "npm run db:reset:test -w api && NODE_ENV=test jest --runInBand",
    "test:tanstack-query": "npm run test -- tanstack_query.ts",
    "lint:check": "eslint",
    "lint:fix": "eslint --fix",
    "format:check": "prettier --check '**/*.{js,ts}'",
    "format:fix": "prettier --write '**/*.{js,ts}'",
    "typecheck": "tsc --noEmit --skipLibCheck --project tsconfig.json",
    "check_puppeteer": "npm run test -- check_puppeteer.ts"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "jest": "^29.7.0",
    "jest-dev-server": "^11.0.0",
    "jest-puppeteer": "^11.0.0",
    "puppeteer": "^24.2.1",
    "ts-jest": "^29.2.5"
  }
}
